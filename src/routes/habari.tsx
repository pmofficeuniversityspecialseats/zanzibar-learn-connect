import { createFileRoute } from "@tanstack/react-router"; import { useMemo,useState } from "react"; import { PageHero } from "@/components/site-shell"; import { SearchBar } from "@/components/content-ui"; import { newsItems } from "@/lib/site-data";
export const Route=createFileRoute("/habari")({head:()=>({meta:[{title:"Habari na Matukio | Ofisi ya Mbunge"},{name:"description",content:"Habari, taarifa, mikutano, ziara na matukio ya ofisi."},{property:"og:title",content:"Habari na Matukio"},{property:"og:description",content:"Taarifa rasmi na shughuli za ofisi."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page}); function Page(){const[q,setQ]=useState("");const list=useMemo(()=>newsItems.filter(x=>`${x.title} ${x.category}`.toLowerCase().includes(q.toLowerCase())),[q]);return <><PageHero eyebrow="Taarifa kwa Umma" title="Habari na Matukio" description="Habari, taarifa, matukio, ziara, mikutano, hotuba na taarifa kwa vyombo vya habari."/><section className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="mb-8 max-w-xl"><SearchBar value={q} onChange={setQ} placeholder="Tafuta habari au aina..."/></div><div className="grid gap-6 lg:grid-cols-3">{list.map(x=><article className="border border-border" key={x.title}><img src={x.image} alt="" className="aspect-video w-full object-cover"/><div className="p-5"><p className="text-xs font-bold text-primary">{x.category} · {x.date}</p><h2 className="mt-3 font-display text-xl font-bold">{x.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{x.summary}</p><p className="mt-4 text-xs font-semibold text-gold">Maelezo kamili yatasasishwa baada ya uthibitisho</p></div></article>)}</div></section></>}