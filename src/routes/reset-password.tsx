import { createFileRoute } from "@tanstack/react-router"; import { useState,type FormEvent } from "react"; import { toast } from "sonner"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Weka Nenosiri Jipya | Ofisi ya Mbunge"},{name:"description",content:"Weka nenosiri jipya kwa akaunti yako."},{property:"og:title",content:"Weka Nenosiri Jipya"},{property:"og:description",content:"Urejeshaji salama wa akaunti."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const[done,setDone]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!window.location.hash.includes("type=recovery")){toast.error("Kiungo cha kurejesha nenosiri si sahihi au kimekwisha muda.");return}const fd=new FormData(e.currentTarget);const p=String(fd.get("password"));const c=String(fd.get("confirm"));if(p!==c){toast.error("Manenosiri hayafanani.");return}const{error}=await supabase.auth.updateUser({password:p});if(error)toast.error(error.message);else setDone(true);}return <section className="mx-auto min-h-[60vh] max-w-md px-4 py-16"><h1 className="font-display text-3xl font-bold">Weka Nenosiri Jipya</h1>{done?<p className="mt-5 text-muted-foreground">Nenosiri limebadilishwa. Unaweza kuingia kwenye akaunti yako.</p>:<form onSubmit={submit} className="mt-8 space-y-5"><div><Label htmlFor="password">Nenosiri jipya</Label><Input id="password" name="password" type="password" minLength={8} required className="mt-2"/></div><div><Label htmlFor="confirm">Rudia nenosiri</Label><Input id="confirm" name="confirm" type="password" minLength={8} required className="mt-2"/></div><Button className="w-full">Hifadhi Nenosiri</Button></form>}</section>}