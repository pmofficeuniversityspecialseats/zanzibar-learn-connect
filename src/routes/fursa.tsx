import { createFileRoute } from "@tanstack/react-router"; import { useMemo,useState } from "react"; import { CalendarDays, ExternalLink } from "lucide-react"; import { PageHero } from "@/components/site-shell"; import { SearchBar } from "@/components/content-ui"; import { Button } from "@/components/ui/button"; import { opportunities } from "@/lib/site-data";
export const Route=createFileRoute("/fursa")({head:()=>({meta:[{title:"Fursa za Elimu na Maendeleo | Zanzibar"},{name:"description",content:"Scholarships, internships, utafiti, ubunifu, kazi na maendeleo ya vijana."},{property:"og:title",content:"Fursa kwa Wanafunzi"},{property:"og:description",content:"Tafuta fursa zilizothibitishwa kwa wanafunzi na vijana."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page}); function Page(){const[q,setQ]=useState("");const list=useMemo(()=>opportunities.filter(x=>`${x.title} ${x.sw}`.toLowerCase().includes(q.toLowerCase())),[q]);return <><PageHero eyebrow="Elimu na Maendeleo" title="Fursa" description="Fursa za ufadhili, mafunzo, utafiti, ubunifu, ujasiriamali na kazi zitakazothibitishwa na taasisi husika."/><section className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="max-w-xl"><SearchBar value={q} onChange={setQ} placeholder="Tafuta kwa aina au kichwa..."/></div><div className="mt-8 grid gap-5 md:grid-cols-2">{list.map(x=><article key={x.title} className="border border-border p-6"><p className="text-xs font-bold uppercase text-gold">Mfano — inasubiri uthibitisho</p><h2 className="mt-3 font-display text-xl font-bold">{x.sw}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{x.description}</p><div className="mt-5 flex items-center gap-2 border-t border-border pt-4 text-xs text-muted-foreground"><CalendarDays className="size-4"/>Mwisho: {x.deadline}</div><Button disabled variant="outline" className="mt-5">Tazama maelezo <ExternalLink/></Button></article>)}</div></section></>}